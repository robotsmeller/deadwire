/* Block comments only. ScriptParser.stripComments removes slash-star pairs
   and nothing else, so a double-slash line is left in as text and becomes
   part of the next block's header. A header that starts with anything but
   `module` is skipped whole, with no error: that is why all four sounds
   were nil in Part H, session 29. The module wrapper matches all 150
   vanilla sound scripts. verify_names.py refuses a double-slash here. */
module Base
{
    sound Deadwire_TinCanRattle
    {
        category = Item,
        is3D = true,
        clip
        {
            file = media/sound/tin_can_rattle.ogg,
            distanceMax = 200,
        }
    }

    sound Deadwire_WireRattle
    {
        category = Item,
        is3D = true,
        clip
        {
            file = media/sound/wire_rattle.ogg,
            distanceMax = 200,
        }
    }

    sound Deadwire_BellRing
    {
        category = Item,
        is3D = true,
        clip
        {
            file = media/sound/bell_ring.ogg,
            distanceMax = 300,
        }
    }

    /* Phase 3 (#13). The clip is here now so the sound name resolves; the wire
       type that plays it does not exist yet. EventHandlers' ElectricZap handler
       already names it, and without this block it would have played nothing at
       all, silently, on the day #13 landed. */
    sound Deadwire_ElecZap
    {
        category = Item,
        is3D = true,
        clip
        {
            file = media/sound/electric_zap.ogg,
            distanceMax = 200,
        }
    }
}
